---
name: update_drop_list_info
pack: bird
runner_type: python-script
description: Retrieves (and store) DROP list from the source urls specified in the pack configuration file
enabled: true
entry_point: update_drop_list_info.py
parameters:
  ip_version:
    type: string
    description: Prefixes IP version (ipv4, ipv6)
    default: ipv4